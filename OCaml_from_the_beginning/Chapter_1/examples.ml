1 + 2 * 3;; (* results in 7 as multiplication has more precendece*)


5 * -2;;

(* modulo operator is mod *)

99>100;; (*results in a boolean*)


(* <> is the not equal to opearator*)


(* opeartors are left associative*)

(* && has higher precendece than || *)

if 100<99
then true
else false;;

